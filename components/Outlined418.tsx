// components/Outlined418.tsx
import styles from './Outlined418.module.css';

export function Outlined418({ className }: { className?: string }) {
  return (
    <span className={`${styles.numeral} ${className ?? ''}`} aria-hidden>
      418
    </span>
  );
}
